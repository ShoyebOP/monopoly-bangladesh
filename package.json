{
  "name": "monopoly-bangladesh",
  "version": "0.1.0",
  "description": "A lightweight web-based Monopoly game optimized for low-end Android devices, featuring Bangladeshi cities and language",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "test": "CI=true node --test tests/**/*.test.js",
    "lint": "CI=true eslint src/**/*.js cli/**/*.js public/**/*.js 2>/dev/null || true",
    "format": "prettier --write src/ cli/ public/"
  },
  "keywords": [
    "monopoly",
    "bangladesh",
    "board-game",
    "multiplayer",
    "termux"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "commander": "^12.0.0",
    "express": "^4.19.0",
    "sql.js": "^1.10.0",
    "ws": "^8.17.0"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "globals": "^17.4.0",
    "prettier": "^3.2.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
